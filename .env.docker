# Docker Environment Configuration for CSM Copilot
# Copy this file to .env in the root directory and update with your values

# Pinecone Configuration
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_ENVIRONMENT=us-east-1-aws
PINECONE_INDEX_NAME=csm-pilot
PINECONE_INDEX_HOST=https://csm-pilot-w9uksjc.svc.aped-4627-b74a.pinecone.io

# Database Configuration (PostgreSQL)
POSTGRES_DB=csmpilot
POSTGRES_USER=csmpilot_user
POSTGRES_PASSWORD=csmpilot_password

# Django Configuration
DJANGO_SECRET_KEY=your-super-secret-django-key-change-in-production
DEBUG=True

# Redis Configuration (usually default is fine)
REDIS_URL=redis://redis:6379/0

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_APP_NAME=CSM Copilot

# SSL Configuration (handled automatically in Docker)
PYTHONHTTPSVERIFY=1